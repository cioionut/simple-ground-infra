# simple-ground-infra
Install k3s cluster on your infra using terraform and ansible

# Todo
- Take a look on `providers.tf`. We can move the provider into the implemention repo and then the terraform block into another file.
- Add more flexibility to the vm operating system.
